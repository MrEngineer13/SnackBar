apply plugin: 'com.android.library'

android {
    compileSdkVersion 20
    buildToolsVersion "20.0.0"

    defaultConfig {
        applicationId "com.mrengineer13.snackbar"
        minSdkVersion 8
        targetSdkVersion 20
        versionCode 2
        versionName "0.2.0"
    }
}
